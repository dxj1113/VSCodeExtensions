import { TrieNode } from './TrieNode';
export interface SuggestionResult {
    word: string;
    cost: number;
}
export declare function suggest(root: TrieNode, word: string, maxNumSuggestions?: number): SuggestionResult[];
