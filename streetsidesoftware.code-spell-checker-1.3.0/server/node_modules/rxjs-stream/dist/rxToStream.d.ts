/// <reference types="node" />
import * as Rx from 'rxjs/Rx';
import * as stream from 'stream';
export declare type Streamable = string | Buffer;
/**
 * Transform the output of an Observable into a node readable stream.
 */
export declare function rxToStream<T extends Streamable>(src: Rx.Observable<T>, options?: stream.ReadableOptions): stream.Readable;
